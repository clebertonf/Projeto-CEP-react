const INITIAL_STATE = [];

function searchCep(state = INITIAL_STATE, action) {
    switch(action.type){
        case '':
            return state;
        default:
          return state;
    }
}

export default searchCep;